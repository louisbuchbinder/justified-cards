{
  "name": "justified-cards",
  "version": "0.0.1",
  "description": "Justified Layout of Rectangular Cards",
  "main": "index.js",
  "scripts": {
    "lint": "eslint . --cache --fix && node package.json",
    "jsdoc": "rm -rf jsdocs && jsdoc . -d jsdocs",
    "test": "jenkins-mocha test --recursive"
  },
  "engines": {
    "node": ">=4"
  },
  "dependencies": {
    "flconf": "^2.2.0",
    "json5": "^0.5.1"
  },
  "devDependencies": {
    "eslint": "^4.1.1",
    "eslint-config-flickr": "^1.3.1",
    "jenkins-mocha": "^4.1.2",
    "jsdoc": "^3.4.3"
  }
}
